# Agent 协作规则

开始前阅读 README.md、docs/requirements.md、common/protocol.md、docs/decisions.md，以及当前模块 README。

- 本仓库是协作骨架，不得宣称已有仿真、调度、固件或 Qt 界面。
- 作业原文优先于整理稿；发现差异先记录并询问，不用推测覆盖要求。
- A/B/C 目录是主要分工，不是权限边界；可为完成任务直接跨目录实现、修复和补测试，无需逐次请求目录负责人授权。跨模块影响、公共协议、字段单位和控制权变更必须在提交说明与 docs/decisions.md 中留痕，并及时通知相关成员。
- A 负责实际仿真，B 负责功率目标，STM32 负责风机动作。禁止用目标冒充实际出力，禁止把调桨效果直接写成负荷变化。
- 不把 C 上位机当作 STM32 的替代品；无硬件测试明确标注 mock。
- 检查 git status，保留用户已有改动；不要强推、重置他人提交或自行合并 PR。
- 不上传 config.local.json、Wi-Fi 密码、令牌、真实数据库；不要在日志中打印密码。
- 未确定参数见 docs/decisions.md；不得把示例值变成隐含课程要求。
- 使用短事务和独立数据库连接；不要通过共享 SQLite 文件跨电脑通信。
- GUI 不阻塞收发；TCP/串口处理分包、粘包、超时、断线及重复报文。
- 完成后运行相关测试，说明改动、测试结果、硬件验证范围和未完成事项。
